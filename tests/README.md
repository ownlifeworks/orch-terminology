# Tests

Resolver fixtures and validation cases will live here. Include normalization, longest-match, contextual precedence, unknown, and ambiguous-resolution cases.

Run from the repository root:

```powershell
python -m unittest discover -s tests
```
