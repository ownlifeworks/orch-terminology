[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "orch-terminology"
version = "0.1.0"
description = "Canonical OwnLife Audio orchestral terminology and deterministic alias resolution"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
